{
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "scripts": {
    "install:all": "cd frontend && bun install && cd ../backend && bun install",
    "dev": "concurrently \"cd frontend && bun run dev\" \"cd backend && bun run server.ts\""  }

}
